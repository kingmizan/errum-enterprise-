// --- ui.js ---
// Contains UI helper functions and HTML templates

// Helper to show a toast notification
export const showToast = (message) => {
    // ... (paste showToast function code from original app.js)
};

// Helper to animate counting up numbers
export const animateCountUp = (el, endValue) => {
    // ... (paste animateCountUp function code from original app.js)
};

// All HTML templates for different views
export const templates = {
    dashboard: `...`, // (paste dashboard template string)
    contacts: `...`, // (paste contacts template string)
    'transaction-form': `...`, // (paste transaction-form template string)
    statements: `...` // (paste statements template string)
};
